const TYPE = {
  GOMSIN: "gomsin",
  SOLDIER: "soldier",
};

const NUMBER = {
  ONEDAY: 1000 * 60 * 60 * 24,
};

const MESSAGE = {
  AUTHORIZED: "user authorized successfully",
};

module.exports = { NUMBER, MESSAGE, TYPE };
